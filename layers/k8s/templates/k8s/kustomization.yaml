apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

# The `app` placeholder the deployment carries; CI pins newTag to the commit sha at deploy time.
images:
  - name: app
    newName: ${registry.image_name_base}
    newTag: latest
